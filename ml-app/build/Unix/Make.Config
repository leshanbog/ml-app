BUILD=release

CC=g++
STD=-std=c++14
SRC=./../src

ifeq ($(BUILD),debug)
mkdir Debug
BUILD_DIR=./Debug
OPTFLAGS=-g -O0
else
mkdir Release
BUILD_DIR=./Release
OPTFLAGS=-O2
endif

CFLAGS=$(STD) $(OPTFLAGS) -Wall -c
